import React from 'react';
import { Card, CardHeader, Avatar, CardContent, Typography, Box } from '@mui/material';

const testimonials = [
  {
    avatar: 'https://via.placeholder.com/60',
    name: 'Dominic Smith',
    title: 'Software Engineer at DOMI',
    feedback:
      'DOMI has completely transformed the way our team collaborates. The platform is intuitive, reliable, and packed with features that save us time every day!',
  },
  {
    avatar: 'https://via.placeholder.com/60',
    name: 'Sophia Johnson',
    title: 'Product Manager at DOMI',
    feedback:
      'Using DOMI has been a game-changer for our workflows. It’s streamlined everything and made our team more efficient than ever before!',
  },
  {
    avatar: 'https://via.placeholder.com/60',
    name: 'Sophia Johnson',
    title: 'Product Manager at DOMI',
    feedback:
      'Using DOMI has been a game-changer for our workflows. It’s streamlined everything and made our team more efficient than ever before!',
  },
];

const TestimonialCards = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'flex-end', // Aligns cards to the right
        alignItems: 'center', // Centers the cards vertically
        width: '100%',
        height: '100vh', // Full height of the viewport
        backgroundImage: 'url("https://via.placeholder.com/1500")', // Set your background image URL here
        backgroundSize: 'cover', // Cover the entire container with the background image
        backgroundPosition: 'center', // Center the background image
        padding: 3, // Optional: Adjust padding for spacing
      }}
    >
      <Box
        sx={{
          display: 'flex',
          gap: 2, // Space between the cards
          flexWrap: 'wrap', // Ensures responsiveness
          maxWidth: '80%', // Set a maximum width for the card container
        }}
      >
        {testimonials.map((testimonial, index) => (
          <Card
            key={index}
            sx={{
              borderRadius: '12px',
              padding: 2,
              height: '200px', // Set fixed height for cards
              display: 'flex',
              flexDirection: 'column',
              maxWidth: '450px',
              border: '1px solid rgba(0, 0, 0, 0.12)',
              backgroundColor: 'white',
              boxShadow: 3, // Optional: Add shadow for visibility
            }}
          >
            <CardHeader
              avatar={
                <Avatar
                  src={testimonial.avatar}
                  alt={testimonial.name}
                  sx={{ width: 60, height: 60 }}
                />
              }
              title={
                <Typography variant="subtitle1" sx={{ fontWeight: 'bold' }}>
                  {testimonial.name}
                </Typography>
              }
              subheader={
                <Typography variant="body2" sx={{ color: 'rgba(0, 0, 0, 0.6)' }}>
                  {testimonial.title}
                </Typography>
              }
            />
            <CardContent sx={{ flexGrow: 1 }}>
              <Typography
                variant="body2"
                sx={{
                  color: 'rgba(0, 0, 0, 0.8)',
                  lineHeight: 1.6,
                  display: '-webkit-box',
                  WebkitLineClamp: 3,
                  WebkitBoxOrient: 'vertical',
                  overflow: 'hidden',
                }}
              >
                {testimonial.feedback}
              </Typography>
            </CardContent>
          </Card>
        ))}
      </Box>
    </Box>
  );
};

export default TestimonialCards;
